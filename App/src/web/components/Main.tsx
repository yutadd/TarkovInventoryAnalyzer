import { useContext, useEffect} from "react";
import { AppContext } from "../App";
import './Main.css'
export const Sidebar = () => {
    const context=useContext(AppContext)
    useEffect(() => {
        
    }, []);
    return (
        <div className="Main">
            {/* <img src= className="Logo"></div> */}
        </div>
    );
};